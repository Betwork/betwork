require 'rails_helper'

RSpec.describe "Users", type: :request do
  # before { allow(controller).to receive(:authenticate_request!).and_return(true) }
  # it "should be able to update a users balance" do
  #   @u1 = User.create!(name: 'Betty', email: 'Betty@betwork.com', password: 'password', actualBalance: 100, balanceInEscrow: 0)
  #   @params = { :id => @u1, :balance_change => 10,
  #               :balance_change_type => "Add",
  #               :user => { :actualBalance => @u1.actualBalance } }
  #
  #   allUsers = User.all #refresh allUsers
  #   expect(allUsers[0].actualBalance).to eq(110)
  # end

end


